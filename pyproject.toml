[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ndgnuh-dev-env"
version = "1.0.2"
dependencies = ["docker"]

[tool.setuptools]
packages = ["ndgnuh_dev_env"]

[project.scripts]
enter-dev="ndgnuh_dev_env.enterdev:main"
